Contains various experiments with a lego mindstorm robot building pack.

Python should be indented with two spaces _not_ tabs.

When performing commits we prefer conventional commit style with 'chore:' 'fix:' and 'feat:' prefixes to the message.
